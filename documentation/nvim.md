# _[MolniOS](https://codeberg.org/al1h3n/molnios-install)_ neovim custom keybinds:

`Main + plus/minus` - change scale of application (related to `kitty` configuration.)<md>

#### LazyVim note
To add __LSP__ clients to `LazyVim`, you should add specified packages to your system and add `lang.x` in `:LazyExtras`.